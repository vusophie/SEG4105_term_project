## Installation
```
# clone repository
git clone https://github.com/vusophie/SEG4105_term_project.git

# install dependencies
yarn install

# for Windows, use this command:
set NODE_OPTIONS=--openssl-legacy-provider

# serve with hot reload at localhost:3000
yarn dev
```
